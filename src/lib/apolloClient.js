import { ApolloClient, InMemoryCache } from "@apollo/client";

// const client = new ApolloClient({
//     uri: "http://localhost:1337/graphql",
//     cache: InMemoryCache(),
// });
const client = "ApolloFunction";
export default client;